document.addEventListener('DOMContentLoaded',()=>{const c=window.SITE_CONFIG||{};const $=s=>document.querySelector(s);const set=(id,v)=>{const e=$(id);if(e&&v)e.textContent=v};set('.brand',c.name||'[NOME DO ESCRITÓRIO]');set('#phoneDisplay',c.phone);set('#whatsappDisplay',c.phone);set('#addressDisplay',c.address);set('#hoursDisplay',c.hours);set('#mapCity',c.city);set('#year',new Date().getFullYear());const menu=$('#menu'),nav=$('#nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelectorAll('.whatsapp,.btn-gold').forEach(()=>{});const buttons=document.querySelectorAll('a[href="#contato"]');buttons.forEach(b=>b.addEventListener('click',()=>{}));const contactBtn=document.querySelector('.contact .btn');if(contactBtn)contactBtn.href=c.mapUrl||'#';document.querySelectorAll('.article-grid a').forEach(a=>a.addEventListener('click',e=>e.preventDefault()));});
