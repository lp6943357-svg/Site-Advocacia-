document.addEventListener('DOMContentLoaded',()=>{const c=window.SITE_CONFIG||{};const $=s=>document.querySelector(s);const setText=(s,v)=>{const e=$(s);if(e&&v)e.textContent=v};const brand=$('.brand');if(brand&&c.name)brand.innerHTML=`${c.name}<span> ADVOCACIA</span>`;setText('#phoneDisplay',c.phone);setText('#whatsappDisplay',c.phone);setText('#addressDisplay',c.address);setText('#hoursDisplay',c.hours);setText('#mapCity',c.city);setText('#year',new Date().getFullYear());const digits=String(c.whatsappNumber||'').replace(/\D/g,'');const whatsappUrl=digits?`https://wa.me/${digits}?text=${encodeURIComponent(c.whatsappMessage||'Olá! Gostaria de falar com o escritório.')}`:'#contato';document.querySelectorAll('[data-whatsapp]').forEach(a=>{a.href=whatsappUrl;a.target='_blank';a.rel='noopener noreferrer'});document.querySelectorAll('[data-map]').forEach(a=>{a.href=c.mapUrl||'#contato';a.target='_blank';a.rel='noopener noreferrer'});const menu=$('#menu'),nav=$('#nav');menu?.addEventListener('click',()=>{const open=nav?.classList.toggle('open');menu.setAttribute('aria-expanded',String(Boolean(open)))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelectorAll('.article-grid a[href="#"]').forEach(a=>a.addEventListener('click',e=>e.preventDefault()));const form=$('#contactForm'),status=$('#formStatus');form?.addEventListener('submit',e=>{e.preventDefault();if(!digits){if(status)status.textContent='O WhatsApp ainda não foi configurado. Adicione o número real no config.js.';return}const data=new FormData(form);const name=String(data.get('name')||'').trim();const phone=String(data.get('phone')||'').trim();const email=String(data.get('email')||'').trim();const subject=String(data.get('subject')||'').trim();const message=String(data.get('message')||'').trim();const text=`Olá! Gostaria de entrar em contato com o escritório.%0A%0ANome: ${name}%0ATelefone: ${phone}${email?`%0AE-mail: ${email}`:''}%0AAssunto: ${subject}%0A%0AMensagem:%0A${message}`;window.open(`https://wa.me/${digits}?text=${text}`,'_blank','noopener,noreferrer');if(status)status.textContent='WhatsApp aberto. Revise a mensagem e toque em enviar.';form.reset()})});
